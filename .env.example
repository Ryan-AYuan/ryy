# Copy to .env and fill in a Fine-grained PAT.
# Required permission: Contents = Read and write (this repo only).
# Do not commit .env.
GITHUB_PUSH_TOKEN=
GITHUB_PUSH_TOKEN_EXPIRES=2026-12-31
# Short family codes for upload.html (comma-separated). Rebuild with:
# node scripts/build-upload-auth.mjs
UPLOAD_ALIASES=9777,2347
